# Ignorar binarios pesados compilados de Windows (se distribuyen vía GitHub Releases)
*.exe
*.msi
*.zip

# Preservar notas y documentación interna
!notas.md
!.gitignore

# Duración de cada paso del último build (build-windows.ps1 lo usa para el porcentaje)
.tiempos-build.json
