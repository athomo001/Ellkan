[workspace]
resolver = "2"
members = ["crates/ellkan-crypto", "backend", "cli"]

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-only"
repository = "https://github.com/athomo001/Ellkan"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
